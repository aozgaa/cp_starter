This repo/dir is a collection of one-off implementations of data structures, combined with test code in single executables.

Convert this to the cmake structure described in /home/fozga/r/art/interviews/stub
(that is, separate libraries/headers from impls, use file sets for libraries instead of include_dirs, ...).
Note that we don't need integration tests since there will be no executables (only test executables).
